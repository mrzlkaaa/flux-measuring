version: "3"
services:
  detectors:
    container_name: detectors
    build: 
      context: ./python
      dockerfile: detec.Dockerfile
    environment:
      - FLASK_APP=detectors_app.py
      - PSQL_USER=${PSQL_USER}
      - PSWD=${PSWD}
      - PORT=${PORT}
      - HOST=${HOST}
      - DB=${DB}
    ports:
      - 5000:5000
    networks:
      - flux_meas_net
  # lib:
  #   container_name: lib
  #   build: ./python/detec_lib
  #   environment:
  #     - FLASK_APP=detec_lib_app.py
  #     - PSQL_USER=${PSQL_USER}
  #     - PSWD=${PSWD}
  #     - PORT=${PORT}
  #     - HOST=${HOST}
  #     - DB_lib=${DB_lib}
  #   ports:
  #     - 5001:5001

  greetings:
    container_name: greetings
    build: 
      context: ./python
      dockerfile: greet.Dockerfile
    environment:
      - FLASK_APP=app.py
    ports:
      - 5002:5002
    networks:
      - flux_meas_net
  
  # proxy:
  #   build: ./nginx
  #   restart: always
  #   ports:
  #     - 80:80
  #   networks:
  #     - flux_meas_net
  
  api:
    container_name: api
    image: flux_meas_api
    restart: always
    ports:
     - 8080:8080
    networks:
      - flux_meas_net

networks:
  flux_meas_net: 